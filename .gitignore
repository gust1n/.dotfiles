bak
.claude/settings.local.json

# Machine-specific layers. The jj one holds a work identity: this repo is public.
config/mise/config.local.toml
config/jj/conf.d/

# Cloned or generated by the tools that own them.
# [bootstrap.repos] clones tpm and the tmux plugins into config/tmux/plugins.
config/tmux/plugins/
config/jj/repos/
config/herdr/session.json
config/herdr/plugins/
config/herdr/plugins.json
config/herdr/release-notes.json
config/herdr/.plugins.lock
config/herdr/*.sock
